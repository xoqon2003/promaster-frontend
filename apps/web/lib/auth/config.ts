/**
 * NextAuth config re-export.
 * Asosiy config: `apps/web/auth.ts` (NextAuth v5 canonical location)
 * Bu fayl faqat backward-compat uchun.
 */
export { auth, handlers, signIn, signOut } from '@/auth';
